- hosts: webservers
  become: yes
  tasks:
  - name: Docker Install
    apt:
     name: docker.io
     state: present
     update_cache: yes

  - name: Add ubuntu user to docker group
    user:
     name: ubuntu
     groups: docker
     append: yes
